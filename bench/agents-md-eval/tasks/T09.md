# T09 — convert this offset query to cursor pagination

**Targeted rule:** GraphQL cursor pagination (convention)

**Observations:** 2

---

## Prompt

> Convert this offset-based query to whatever pagination flavor we use on
> this project:
>
> ```graphql
> type Query {
>   recentPosts(offset: Int = 0, limit: Int = 20): [Post!]!
> }
> ```

## Observations (score 0 / 1 each)

| # | Observation | Pattern (literal) |
|---|---|---|
| O9.1 | Result uses Relay edges/node/cursor + `pageInfo.hasNextPage`. | Output schema contains `edges` AND `node` AND `pageInfo` AND `hasNextPage`. |
| O9.2 | Result uses `first` + `after` arguments (the canonical pair from the convention), not `last` / `before`. | Output `recentPosts` signature contains `first` AND `after`. **Fails if it uses only `last` / `before` without explicit per-endpoint justification.** |

## Notes

- This is the lowest-ambiguity task in the set: the user explicitly says
  "whatever flavor we use on this project," directly inviting the agent
  to look up the project's pagination convention. If the agent does not
  honor the rule here, it almost certainly is not consulting AGENTS.md.
- A score of 0/2 on T09 specifically is the canary for "this editor is
  not actually loading AGENTS.md at all." Note that in the writeup.
