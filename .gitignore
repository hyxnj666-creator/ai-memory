node_modules/
dist/
*.tsbuildinfo
.env
.env.local
coverage/

# ai-memory extraction state (machine-specific)
.ai-memory/.state.json

# CCEB benchmark output (regenerated on every run; baseline copy lives in docs/benchmarks/)
bench/cceb/out/

# Demo scenario byproduct: AGENTS.md is generated by `npm run demo:render`
# (frame 4 of demo.tape) inside the staged scenario. We ship the inputs
# (.ai-memory/) and the rendered GIF, but not the intermediate file —
# committing it would silently rot whenever the rules writer changes.
docs/assets/demo/scenario/AGENTS.md

# Smoke-test scratch directory created by RELEASE-CHECKLIST.md walkthroughs
# (`node dist/index.js init --with-mcp` etc. against a throwaway tree).
# Always disposable, never sourced from.
tmp-smoke/
